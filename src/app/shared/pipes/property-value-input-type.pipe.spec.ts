import { PropertyValueInputTypePipe } from './property-value-input-type.pipe';

describe('PropertyValueInputTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyValueInputTypePipe();
    expect(pipe).toBeTruthy();
  });
});
